<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.DayNight.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorOrangePrimary</item>
        <item name="colorPrimaryDark">@color/colorOrangrPrimaryDark</item>
        <item name="colorAccent">@color/colorRedAccent</item>
        <item name="android:textColorPrimary">@android:color/white</item>
    </style>

    <style name="MySwitch">
    <item name="colorControlActivated">@color/colorRedAccent</item>
    </style>

</resources>
